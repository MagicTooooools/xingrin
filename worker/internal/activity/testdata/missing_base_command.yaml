metadata:
  name: "test_workflow"
  version: "1.0.0"
  stages:
    - id: "recon"
      name: "Reconnaissance"
      order: 1

tools:
  subfinder:
    metadata:
      display_name: "Subfinder"
      stage: "recon"
    # base_command 缺失
    cli_params: []
